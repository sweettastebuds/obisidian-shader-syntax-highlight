name: Build

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci --legacy-peer-deps
      - run: npm run build
      - name: Create zip archive
        run: zip obsidian-shader-syntax-highlight.zip main.js manifest.json styles.css
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: obsidian-shader-syntax-highlight
          path: obsidian-shader-syntax-highlight.zip
          retention-days: 3
